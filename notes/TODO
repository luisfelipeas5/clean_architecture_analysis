[X] - Position the nodes properly
[X] - Paint arrows with a different color when clean archtecture rule is violated
[X] - Fade nodes that aren't in focus or has no edge with which is in focus
[X] - When click on nodes, remove from graph everything else that don't be part of the sub-graph
[X] - Add background color to nodes based on Clean Architecture order
[ ] - Show which files are violating the arch rule
[ ] - Hide arrows when node are not selected
[ ] - Show cycles in dependency graph
[ ] - Show stability of each component